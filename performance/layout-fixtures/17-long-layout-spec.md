# Synthetic long specification

This generated twenty-section document is a layout and editing stress fixture, not the user's Nudge specification. All fields and examples below are synthetic.

## 1. Synthetic section

### Runtime properties

| Property | Value |
| --- | ---: |
| Timeout | 30 s |
| Workers | 4 |
| Cache | 64 MiB |

### Environment comparison

| Environment | Endpoint | Retry |
| --- | --- | ---: |
| Local | localhost | 2 |
| Staging | staging.example.test | 3 |

### Independent checks

- **Identity:** Keep canonical source nodes.
- **Order:** Read groups in source sequence.
- **Selection:** Copy the authored content.
- **Editing:** Hold the focused column steady.
- **Sizing:** Measure with the actual fonts.
- **Fallback:** Keep all content accessible.

### Detailed requirements

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

### Field traceability

| Field | Requirement | Verification |
| --- | --- | --- |
| S1-F1 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S1-F2 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S1-F3 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S1-F4 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S1-F5 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S1-F6 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S1-F7 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S1-F8 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S1-F9 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S1-F10 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S1-F11 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S1-F12 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |

## 2. Synthetic section

### Runtime properties

| Property | Value |
| --- | ---: |
| Timeout | 30 s |
| Workers | 4 |
| Cache | 64 MiB |

### Environment comparison

| Environment | Endpoint | Retry |
| --- | --- | ---: |
| Local | localhost | 2 |
| Staging | staging.example.test | 3 |

### Independent checks

- **Identity:** Keep canonical source nodes.
- **Order:** Read groups in source sequence.
- **Selection:** Copy the authored content.
- **Editing:** Hold the focused column steady.
- **Sizing:** Measure with the actual fonts.
- **Fallback:** Keep all content accessible.

### Detailed requirements

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

### Field traceability

| Field | Requirement | Verification |
| --- | --- | --- |
| S2-F1 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S2-F2 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S2-F3 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S2-F4 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S2-F5 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S2-F6 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S2-F7 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S2-F8 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S2-F9 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S2-F10 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S2-F11 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S2-F12 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |

## 3. Synthetic section

### Runtime properties

| Property | Value |
| --- | ---: |
| Timeout | 30 s |
| Workers | 4 |
| Cache | 64 MiB |

### Environment comparison

| Environment | Endpoint | Retry |
| --- | --- | ---: |
| Local | localhost | 2 |
| Staging | staging.example.test | 3 |

### Independent checks

- **Identity:** Keep canonical source nodes.
- **Order:** Read groups in source sequence.
- **Selection:** Copy the authored content.
- **Editing:** Hold the focused column steady.
- **Sizing:** Measure with the actual fonts.
- **Fallback:** Keep all content accessible.

### Detailed requirements

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

### Field traceability

| Field | Requirement | Verification |
| --- | --- | --- |
| S3-F1 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S3-F2 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S3-F3 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S3-F4 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S3-F5 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S3-F6 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S3-F7 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S3-F8 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S3-F9 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S3-F10 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S3-F11 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S3-F12 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |

## 4. Synthetic section

### Runtime properties

| Property | Value |
| --- | ---: |
| Timeout | 30 s |
| Workers | 4 |
| Cache | 64 MiB |

### Environment comparison

| Environment | Endpoint | Retry |
| --- | --- | ---: |
| Local | localhost | 2 |
| Staging | staging.example.test | 3 |

### Independent checks

- **Identity:** Keep canonical source nodes.
- **Order:** Read groups in source sequence.
- **Selection:** Copy the authored content.
- **Editing:** Hold the focused column steady.
- **Sizing:** Measure with the actual fonts.
- **Fallback:** Keep all content accessible.

### Detailed requirements

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

### Field traceability

| Field | Requirement | Verification |
| --- | --- | --- |
| S4-F1 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S4-F2 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S4-F3 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S4-F4 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S4-F5 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S4-F6 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S4-F7 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S4-F8 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S4-F9 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S4-F10 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S4-F11 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S4-F12 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |

## 5. Synthetic section

### Runtime properties

| Property | Value |
| --- | ---: |
| Timeout | 30 s |
| Workers | 4 |
| Cache | 64 MiB |

### Environment comparison

| Environment | Endpoint | Retry |
| --- | --- | ---: |
| Local | localhost | 2 |
| Staging | staging.example.test | 3 |

### Independent checks

- **Identity:** Keep canonical source nodes.
- **Order:** Read groups in source sequence.
- **Selection:** Copy the authored content.
- **Editing:** Hold the focused column steady.
- **Sizing:** Measure with the actual fonts.
- **Fallback:** Keep all content accessible.

### Detailed requirements

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

### Field traceability

| Field | Requirement | Verification |
| --- | --- | --- |
| S5-F1 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S5-F2 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S5-F3 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S5-F4 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S5-F5 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S5-F6 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S5-F7 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S5-F8 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S5-F9 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S5-F10 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S5-F11 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S5-F12 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |

## 6. Synthetic section

### Runtime properties

| Property | Value |
| --- | ---: |
| Timeout | 30 s |
| Workers | 4 |
| Cache | 64 MiB |

### Environment comparison

| Environment | Endpoint | Retry |
| --- | --- | ---: |
| Local | localhost | 2 |
| Staging | staging.example.test | 3 |

### Independent checks

- **Identity:** Keep canonical source nodes.
- **Order:** Read groups in source sequence.
- **Selection:** Copy the authored content.
- **Editing:** Hold the focused column steady.
- **Sizing:** Measure with the actual fonts.
- **Fallback:** Keep all content accessible.

### Detailed requirements

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

### Field traceability

| Field | Requirement | Verification |
| --- | --- | --- |
| S6-F1 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S6-F2 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S6-F3 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S6-F4 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S6-F5 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S6-F6 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S6-F7 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S6-F8 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S6-F9 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S6-F10 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S6-F11 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S6-F12 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |

## 7. Synthetic section

### Runtime properties

| Property | Value |
| --- | ---: |
| Timeout | 30 s |
| Workers | 4 |
| Cache | 64 MiB |

### Environment comparison

| Environment | Endpoint | Retry |
| --- | --- | ---: |
| Local | localhost | 2 |
| Staging | staging.example.test | 3 |

### Independent checks

- **Identity:** Keep canonical source nodes.
- **Order:** Read groups in source sequence.
- **Selection:** Copy the authored content.
- **Editing:** Hold the focused column steady.
- **Sizing:** Measure with the actual fonts.
- **Fallback:** Keep all content accessible.

### Detailed requirements

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

### Field traceability

| Field | Requirement | Verification |
| --- | --- | --- |
| S7-F1 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S7-F2 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S7-F3 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S7-F4 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S7-F5 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S7-F6 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S7-F7 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S7-F8 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S7-F9 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S7-F10 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S7-F11 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S7-F12 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |

## 8. Synthetic section

### Runtime properties

| Property | Value |
| --- | ---: |
| Timeout | 30 s |
| Workers | 4 |
| Cache | 64 MiB |

### Environment comparison

| Environment | Endpoint | Retry |
| --- | --- | ---: |
| Local | localhost | 2 |
| Staging | staging.example.test | 3 |

### Independent checks

- **Identity:** Keep canonical source nodes.
- **Order:** Read groups in source sequence.
- **Selection:** Copy the authored content.
- **Editing:** Hold the focused column steady.
- **Sizing:** Measure with the actual fonts.
- **Fallback:** Keep all content accessible.

### Detailed requirements

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

### Field traceability

| Field | Requirement | Verification |
| --- | --- | --- |
| S8-F1 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S8-F2 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S8-F3 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S8-F4 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S8-F5 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S8-F6 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S8-F7 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S8-F8 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S8-F9 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S8-F10 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S8-F11 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S8-F12 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |

## 9. Synthetic section

### Runtime properties

| Property | Value |
| --- | ---: |
| Timeout | 30 s |
| Workers | 4 |
| Cache | 64 MiB |

### Environment comparison

| Environment | Endpoint | Retry |
| --- | --- | ---: |
| Local | localhost | 2 |
| Staging | staging.example.test | 3 |

### Independent checks

- **Identity:** Keep canonical source nodes.
- **Order:** Read groups in source sequence.
- **Selection:** Copy the authored content.
- **Editing:** Hold the focused column steady.
- **Sizing:** Measure with the actual fonts.
- **Fallback:** Keep all content accessible.

### Detailed requirements

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

### Field traceability

| Field | Requirement | Verification |
| --- | --- | --- |
| S9-F1 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S9-F2 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S9-F3 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S9-F4 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S9-F5 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S9-F6 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S9-F7 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S9-F8 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S9-F9 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S9-F10 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S9-F11 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S9-F12 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |

## 10. Synthetic section

### Runtime properties

| Property | Value |
| --- | ---: |
| Timeout | 30 s |
| Workers | 4 |
| Cache | 64 MiB |

### Environment comparison

| Environment | Endpoint | Retry |
| --- | --- | ---: |
| Local | localhost | 2 |
| Staging | staging.example.test | 3 |

### Independent checks

- **Identity:** Keep canonical source nodes.
- **Order:** Read groups in source sequence.
- **Selection:** Copy the authored content.
- **Editing:** Hold the focused column steady.
- **Sizing:** Measure with the actual fonts.
- **Fallback:** Keep all content accessible.

### Detailed requirements

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

### Field traceability

| Field | Requirement | Verification |
| --- | --- | --- |
| S10-F1 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S10-F2 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S10-F3 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S10-F4 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S10-F5 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S10-F6 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S10-F7 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S10-F8 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S10-F9 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S10-F10 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S10-F11 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S10-F12 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |

## 11. Synthetic section

### Runtime properties

| Property | Value |
| --- | ---: |
| Timeout | 30 s |
| Workers | 4 |
| Cache | 64 MiB |

### Environment comparison

| Environment | Endpoint | Retry |
| --- | --- | ---: |
| Local | localhost | 2 |
| Staging | staging.example.test | 3 |

### Independent checks

- **Identity:** Keep canonical source nodes.
- **Order:** Read groups in source sequence.
- **Selection:** Copy the authored content.
- **Editing:** Hold the focused column steady.
- **Sizing:** Measure with the actual fonts.
- **Fallback:** Keep all content accessible.

### Detailed requirements

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

### Field traceability

| Field | Requirement | Verification |
| --- | --- | --- |
| S11-F1 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S11-F2 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S11-F3 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S11-F4 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S11-F5 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S11-F6 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S11-F7 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S11-F8 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S11-F9 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S11-F10 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S11-F11 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S11-F12 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |

## 12. Synthetic section

### Runtime properties

| Property | Value |
| --- | ---: |
| Timeout | 30 s |
| Workers | 4 |
| Cache | 64 MiB |

### Environment comparison

| Environment | Endpoint | Retry |
| --- | --- | ---: |
| Local | localhost | 2 |
| Staging | staging.example.test | 3 |

### Independent checks

- **Identity:** Keep canonical source nodes.
- **Order:** Read groups in source sequence.
- **Selection:** Copy the authored content.
- **Editing:** Hold the focused column steady.
- **Sizing:** Measure with the actual fonts.
- **Fallback:** Keep all content accessible.

### Detailed requirements

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

### Field traceability

| Field | Requirement | Verification |
| --- | --- | --- |
| S12-F1 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S12-F2 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S12-F3 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S12-F4 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S12-F5 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S12-F6 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S12-F7 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S12-F8 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S12-F9 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S12-F10 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S12-F11 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S12-F12 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |

## 13. Synthetic section

### Runtime properties

| Property | Value |
| --- | ---: |
| Timeout | 30 s |
| Workers | 4 |
| Cache | 64 MiB |

### Environment comparison

| Environment | Endpoint | Retry |
| --- | --- | ---: |
| Local | localhost | 2 |
| Staging | staging.example.test | 3 |

### Independent checks

- **Identity:** Keep canonical source nodes.
- **Order:** Read groups in source sequence.
- **Selection:** Copy the authored content.
- **Editing:** Hold the focused column steady.
- **Sizing:** Measure with the actual fonts.
- **Fallback:** Keep all content accessible.

### Detailed requirements

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

### Field traceability

| Field | Requirement | Verification |
| --- | --- | --- |
| S13-F1 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S13-F2 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S13-F3 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S13-F4 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S13-F5 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S13-F6 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S13-F7 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S13-F8 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S13-F9 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S13-F10 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S13-F11 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S13-F12 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |

## 14. Synthetic section

### Runtime properties

| Property | Value |
| --- | ---: |
| Timeout | 30 s |
| Workers | 4 |
| Cache | 64 MiB |

### Environment comparison

| Environment | Endpoint | Retry |
| --- | --- | ---: |
| Local | localhost | 2 |
| Staging | staging.example.test | 3 |

### Independent checks

- **Identity:** Keep canonical source nodes.
- **Order:** Read groups in source sequence.
- **Selection:** Copy the authored content.
- **Editing:** Hold the focused column steady.
- **Sizing:** Measure with the actual fonts.
- **Fallback:** Keep all content accessible.

### Detailed requirements

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

### Field traceability

| Field | Requirement | Verification |
| --- | --- | --- |
| S14-F1 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S14-F2 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S14-F3 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S14-F4 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S14-F5 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S14-F6 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S14-F7 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S14-F8 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S14-F9 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S14-F10 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S14-F11 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S14-F12 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |

## 15. Synthetic section

### Runtime properties

| Property | Value |
| --- | ---: |
| Timeout | 30 s |
| Workers | 4 |
| Cache | 64 MiB |

### Environment comparison

| Environment | Endpoint | Retry |
| --- | --- | ---: |
| Local | localhost | 2 |
| Staging | staging.example.test | 3 |

### Independent checks

- **Identity:** Keep canonical source nodes.
- **Order:** Read groups in source sequence.
- **Selection:** Copy the authored content.
- **Editing:** Hold the focused column steady.
- **Sizing:** Measure with the actual fonts.
- **Fallback:** Keep all content accessible.

### Detailed requirements

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

### Field traceability

| Field | Requirement | Verification |
| --- | --- | --- |
| S15-F1 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S15-F2 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S15-F3 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S15-F4 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S15-F5 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S15-F6 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S15-F7 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S15-F8 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S15-F9 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S15-F10 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S15-F11 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S15-F12 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |

## 16. Synthetic section

### Runtime properties

| Property | Value |
| --- | ---: |
| Timeout | 30 s |
| Workers | 4 |
| Cache | 64 MiB |

### Environment comparison

| Environment | Endpoint | Retry |
| --- | --- | ---: |
| Local | localhost | 2 |
| Staging | staging.example.test | 3 |

### Independent checks

- **Identity:** Keep canonical source nodes.
- **Order:** Read groups in source sequence.
- **Selection:** Copy the authored content.
- **Editing:** Hold the focused column steady.
- **Sizing:** Measure with the actual fonts.
- **Fallback:** Keep all content accessible.

### Detailed requirements

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

### Field traceability

| Field | Requirement | Verification |
| --- | --- | --- |
| S16-F1 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S16-F2 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S16-F3 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S16-F4 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S16-F5 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S16-F6 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S16-F7 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S16-F8 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S16-F9 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S16-F10 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S16-F11 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S16-F12 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |

## 17. Synthetic section

### Runtime properties

| Property | Value |
| --- | ---: |
| Timeout | 30 s |
| Workers | 4 |
| Cache | 64 MiB |

### Environment comparison

| Environment | Endpoint | Retry |
| --- | --- | ---: |
| Local | localhost | 2 |
| Staging | staging.example.test | 3 |

### Independent checks

- **Identity:** Keep canonical source nodes.
- **Order:** Read groups in source sequence.
- **Selection:** Copy the authored content.
- **Editing:** Hold the focused column steady.
- **Sizing:** Measure with the actual fonts.
- **Fallback:** Keep all content accessible.

### Detailed requirements

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

### Field traceability

| Field | Requirement | Verification |
| --- | --- | --- |
| S17-F1 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S17-F2 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S17-F3 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S17-F4 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S17-F5 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S17-F6 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S17-F7 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S17-F8 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S17-F9 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S17-F10 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S17-F11 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S17-F12 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |

## 18. Synthetic section

### Runtime properties

| Property | Value |
| --- | ---: |
| Timeout | 30 s |
| Workers | 4 |
| Cache | 64 MiB |

### Environment comparison

| Environment | Endpoint | Retry |
| --- | --- | ---: |
| Local | localhost | 2 |
| Staging | staging.example.test | 3 |

### Independent checks

- **Identity:** Keep canonical source nodes.
- **Order:** Read groups in source sequence.
- **Selection:** Copy the authored content.
- **Editing:** Hold the focused column steady.
- **Sizing:** Measure with the actual fonts.
- **Fallback:** Keep all content accessible.

### Detailed requirements

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

### Field traceability

| Field | Requirement | Verification |
| --- | --- | --- |
| S18-F1 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S18-F2 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S18-F3 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S18-F4 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S18-F5 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S18-F6 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S18-F7 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S18-F8 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S18-F9 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S18-F10 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S18-F11 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S18-F12 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |

## 19. Synthetic section

### Runtime properties

| Property | Value |
| --- | ---: |
| Timeout | 30 s |
| Workers | 4 |
| Cache | 64 MiB |

### Environment comparison

| Environment | Endpoint | Retry |
| --- | --- | ---: |
| Local | localhost | 2 |
| Staging | staging.example.test | 3 |

### Independent checks

- **Identity:** Keep canonical source nodes.
- **Order:** Read groups in source sequence.
- **Selection:** Copy the authored content.
- **Editing:** Hold the focused column steady.
- **Sizing:** Measure with the actual fonts.
- **Fallback:** Keep all content accessible.

### Detailed requirements

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

### Field traceability

| Field | Requirement | Verification |
| --- | --- | --- |
| S19-F1 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S19-F2 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S19-F3 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S19-F4 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S19-F5 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S19-F6 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S19-F7 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S19-F8 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S19-F9 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S19-F10 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S19-F11 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S19-F12 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |

## 20. Synthetic section

### Runtime properties

| Property | Value |
| --- | ---: |
| Timeout | 30 s |
| Workers | 4 |
| Cache | 64 MiB |

### Environment comparison

| Environment | Endpoint | Retry |
| --- | --- | ---: |
| Local | localhost | 2 |
| Staging | staging.example.test | 3 |

### Independent checks

- **Identity:** Keep canonical source nodes.
- **Order:** Read groups in source sequence.
- **Selection:** Copy the authored content.
- **Editing:** Hold the focused column steady.
- **Sizing:** Measure with the actual fonts.
- **Fallback:** Keep all content accessible.

### Detailed requirements

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

This paragraph describes a synthetic requirement used to test continuous reading and stable document editing. The layout must keep every word in its original order while offering a comfortable reading measure. A change to one value must not relocate the active caret into another column. The example deliberately repeats ordinary language so that a long document can be tested without exposing confidential material. Measurement should inspect only the requested region and its nearby planning windows. Content outside that region must remain available for search, selection, copying, navigation and later editing.

### Field traceability

| Field | Requirement | Verification |
| --- | --- | --- |
| S20-F1 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S20-F2 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S20-F3 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S20-F4 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S20-F5 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S20-F6 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S20-F7 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S20-F8 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S20-F9 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S20-F10 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S20-F11 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
| S20-F12 | Preserve the complete synthetic field value in canonical source order. | Edit, copy and undo this field without changing unrelated text. |
